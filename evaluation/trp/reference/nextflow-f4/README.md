# Local TRP engineering reference, F4

The reference passed through `trp_prepare` and `trp_run`, with observed host inventory, the explicit development budget and a **synthetic** `Question.Service.reply` driver. It is not a human/model evaluation. Two stub tasks and two real tasks completed; the 12-unit geometry CSV equals the F2/F3 reference byte for byte.

The `evidence/` directory is a closed portable copy. It includes **121 declared files**, plus `manifest.json`, including Nextflow work/cache/log files and internal staged file links copied as ordinary bytes. Image layers and the shared Nextflow distribution are identified but not embedded. Cache files are evidence, not a portable resume guarantee.

```bash
python3 evidence/verify.py evidence \
  --sha256 1c1a81f55fdf9e3e1d33c0eb3ec045055169e1a738321a5c4bba2affacd71bb3
```

Use the same command against a copied directory; no agent, model, network or scientific runtime is required for integrity verification. Keep the expected manifest hash separately. A consistent manifest alone does not establish authorship or biological correctness.

The fault-injection audit detected 250/250 development faults; see `docs/tesis/evidencia/portable-audit-fase4.json`. Storage admission is an estimate without an enforced aggregate quota. Strict quota requests are rejected. The engineering budget does not set the thesis evaluation or OpenAI budget.
